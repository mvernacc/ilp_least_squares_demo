[project]
name = "ilp-least-squares-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "highspy>=1.9.0",
    "numpy>=1.24.4",
    "pulp>=2.9.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.6",
]
